<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1><%= title %></h1>

  <% if (categories.length === 0) { %>
    <p>Brak kategorii przepisów.</p>
  <% } else { %>
    <ul>
      <% categories.forEach(category => { %>
        <li>
          <a href="/recipes/<%= category.id %>"><%= category.name %></a>
        </li>
      <% }); %>
    </ul>
  <% } %>
</body>
</html>
